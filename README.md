# Alfred Inventory-Assistant
Alfred is an AI inventory management system. Alfred can read, write, and display inventory data while proactively flagging stock shortages and many other functionalities./

To Run\
add .env with you API key\
pip install -r requirements.txt\
python3 -m streamlit run app.py\
